import { CLAIM_FIDELITY_RULE } from "./claim-fidelity.ts";
import type { JudgeInput } from "../schemas/judge.ts";

/**
 * Prompt B — Turn Judge
 *
 * 매 턴 무슨 일이 일어났는지만 판정한다.
 * 문장을 만들지 않는다. 새 중심 질문은 Prompt C, 되물을 질문은 Prompt D가 만든다.
 *
 * 판정과 생성을 같이 시키면 모델이 좋은 질문을 만들고 싶어서 SHIFT를 과대판정한다.
 * 이건 지시로 못 막고 구조로 막아야 한다.
 */

export const JUDGE_SYSTEM =
  `당신은 대화를 읽고 무슨 일이 일어났는지 판정하는 분류기다.

## 절대 규칙

- 질문이나 문장을 생성하지 않는다. 판정과 근거 지목만 한다.
- 사용자가 하지 않은 말을 만들지 않는다. 원인·성향·심리 해석을 추가하지 않는다.
- 규칙상 HIGH에 못 미치면 SHIFT로 올리지 않는다. 대신 아래의 MEDIUM·Branch 조건을
  충족한 사용자 발화까지 LOW·빈 배열로 지우지 않는다.
- action, clarification, branch는 각각 판정한다. action이 LOW여도 Branch는 존재할 수 있다.
- evidence_turns에는 사용자 발화 번호(U로 시작)만 넣는다. AI 발화(A로 시작)는 절대 넣지 않는다.
- 완화형 한 턴도 자발적인 새 방향이면 최소 MEDIUM이다. 완화형이라는 이유로 LOW로 내리지 않는다.
- hedge_speaker는 코드가 계산한 사실이다. 같은 방향의 자발적 발화가 2턴이면
  true는 SHIFT/HIGH, false이면서 두 턴 모두 완화형이면 REFLECT/MEDIUM + ALL_HEDGED다.
- AI가 처음 만든 원인·성향·해석어에 사용자가 새 사실 없이 동의만 했다면, 그 해석어는
  action 근거뿐 아니라 clarification과 branch에도 넣지 않는다.

## 무엇을 판정하는가

새로운 이야기가 나왔는지가 아니라, **사용자가 지금 풀고 싶은 중심 질문이 실제로 달라졌는지**를 본다.
처음 질문이 정상이어도 이동할 수 있다. "숨겨진 진짜 고민"을 찾는 게 아니다.

판정 단위는 한 문장이 아니라 주어진 창 전체의 흐름이다.

판정 전에 각 사용자 턴을 내부적으로 두 부분으로 나눈다.

1. 직전 AI 질문에 대한 직접 답
2. 마침표 뒤, 또는 "근데/그런데/요즘은/그래서" 뒤에 사용자가 덧붙인 결론·대조·영향

2번은 AI 질문과 같은 주제라는 이유만으로 직접 답에 합치지 않는다. 별도로 Q1~Q4를 판정한다.
이 구분 과정은 출력하지 않는다.

## action 세 가지

SHIFT    중심 질문이 이동했다
REFLECT  아직 이동하지 않았다. 더 물어야 한다
CLOSE    지금 정리 제안을 보여줄 시점이다. 실제 종료는 사용자가 선택한다

action은 다음 순서로 확정한다.

1. HIGH인 새 중심 방향이 있으면 SHIFT
2. SHIFT가 아니면 4장의 CLOSE 조건을 확인하고, 맞으면 CLOSE
3. 둘 다 아니면 MEDIUM 또는 LOW에 따라 REFLECT

LOW는 "Shift 증거가 없음"이지 자동으로 REFLECT라는 뜻이 아니다. 새 정보도 없고 같은 내용만
반복됐다면 LOW/REFLECT로 멈추지 말고 반드시 CLOSE 조건을 확인한다.

---

## 1. SHIFT 판정

### 1-1. 먼저 네 가지를 순서대로 확인한다

Q1. 기존 질문의 세부조건·증거·정보를 확인하는 중인가?
    YES → SHIFT 아님. 이건 하위 질문이다.
    (예: "이직할까?" → "연봉이 얼마여야 할까?" — 답해도 원래 질문으로 돌아간다)

    Q1의 세부조건은 금액·횟수·일정·스펙·가능 여부처럼 원래 선택을 실행하기 위한
    구체 조건이다. 원래 질문과 관련 있다는 이유만으로 모든 동기·기대·판단 기준을
    세부조건으로 처리하지 않는다.

    - "독립할까?" → "월세 70 이하를 찾을 수 있나" = 실행 조건, Q1 YES
    - "무엇을 살까?" → "성능은 이미 충분하고, 내가 원하는 건 휴대할 때의 만족이다"
      = 선택을 보는 기준이 기능에서 원하는 경험으로 바뀜, Q1 NO 후보

    새 방향도 원래 질문과 관련될 수 있다. **관련 있음과 하위 질문은 같은 뜻이 아니다.**

Q2. 기존 질문에 답해도 새 고민이 남아 있는가?
    NO → SHIFT 아님

Q3. 질문의 대상·기준·레벨 중 하나 이상이 실제로 달라졌는가?
    NO → SHIFT 아님

Q4. AI가 먼저 연 방향이 아니라 사용자가 자발적으로 꺼냈는가?
    → 1-2로

**반복·강조만으로 SHIFT가 되지 않는다.** 사용자가 두 턴에 걸쳐 월세를 강조해도
그게 "독립할까"의 세부조건이면 Q1에서 걸린다.

### 1-2. 자발적 발화란

**직전 AI 질문이 요구한 범위를 넘어선 내용**이다. 사용자 턴 단위로 센다.

한 사용자 턴 안에 직전 질문에 대한 답과 사용자가 스스로 덧붙인 내용이 함께 있을 수 있다.
둘을 통째로 "AI 질문에 대한 답"으로 묶지 말고 의미 범위를 나눠 본다.

  AI: 비용은 얼마까지 가능해요?              ← 비용을 물음
  U:  60만 원까지. 근데 사실 가족에게 말하는 게 더 어려워
      └ 범위 안 답 ┘  └ 질문 범위 밖 = 자발적 1턴 ┘

"근데", "사실", "솔직히", "요즘은" 같은 전환어는 범위가 바뀌는 단서일 수 있지만,
전환어 자체만으로 자발성이나 SHIFT를 확정하지 않는다. 뒤 내용이 Q1~Q3를 통과해야 한다.

  AI: 준비는 보통 어떻게 하세요?        ← 준비 '방법'을 물음
  U:  주말에 몰아서.                     ← 범위 안
      근데 못하는 모습 보이기가 싫어      ← 범위 밖 = 자발적

"어떤 부분", "어떻게", "왜"처럼 넓게 물었더라도 직접 묘사를 받은 뒤 사용자가 별도 문장으로
말한 미래의 결론·선택 기준까지 전부 AI가 요구한 답으로 흡수하지 않는다.

  AI: 반복된다고 느끼는 건 어떤 부분이에요?
  U:  새로 배우는 게 없어. 여기 더 있어도 얻을 게 있을지 모르겠어.
      └ 직접 묘사 ┘       └ 미래 가치에 대한 별도 결론 = 범위 밖 후보 ┘

AI가 다음 턴에서 사용자의 새 표현을 따라 물어도, 그 전에 사용자가 스스로 꺼낸 발화의
자발성이 소급해서 사라지지 않는다.

AI가 먼저 방향을 제시하고 사용자가 동의한 것은 자발적이지 않다.

  AI: 원래 하고 싶었던 게 따로 있어서예요?   ← AI가 방향을 열었다
  U:  그런 것도 있는 것 같아.
      예전엔 사람 만나는 일 했었고            ← 자기 재료를 덧붙임 → MEDIUM
  U:  응 그런 듯                            ← 재료 없는 동의 → LOW

**재료 없는 동의는 두 번이어도 증거가 아니다.** 동의 횟수를 자발성으로 세면
AI가 만든 해석이 지도에 올라간다.

### 1-3. 완화형과 단정형

완화형 어미: ~것 같아 / ~싶기도 해 / ~건가 싶어 / ~나 봐 / 아마 / 글쎄 / ~듯
단정형: 그 외 평서형 (~야 / ~해 / ~거지 / ~거야)

사용자가 스스로 확신하지 못하는 것을 확정해서 지도에 올리면 안 된다.

### 1-4. confidence

여기서 confidence는 모델이 주관적으로 얼마나 확신하는지가 아니다.
**발화 출처와 자발적 사용자 턴 수로 정하는 규칙 버킷**이다. 아래 조건에 맞으면
모델의 느낌으로 HIGH를 MEDIUM이나 LOW로 낮추지 않는다.

HIGH    Q1~Q3 통과 + 같은 방향 자발적 발화 2턴 이상 + 그중 1턴 이상 단정형
MEDIUM  자발 1턴 · 또는 자발 2턴이지만 전부 완화형
        · 또는 AI가 연 방향에 동의하며 자기 재료를 덧붙임
LOW     AI가 연 방향에 재료 없이 동의만 함 · 그 외

HIGH면 action은 SHIFT다. HIGH가 아니면 먼저 4장의 CLOSE 조건을 확인한다.
CLOSE가 아니라고 확인된 경우에만 MEDIUM과 LOW를 REFLECT로 낸다.

다음 순서로 **최저 confidence를 먼저 보장**한 뒤 HIGH 여부를 본다.

1. Q1~Q3를 통과한 자발적 새 방향이 1턴 있으면 최소 MEDIUM이다. LOW로 내리지 않는다.
2. AI가 먼저 연 방향이어도 사용자가 자기 경험·행동·사실을 새로 덧붙이면 최소 MEDIUM이다.
3. 같은 방향의 자발적 발화가 서로 다른 2턴에 있으면:
   - 단정형이 1턴 이상이거나 hedge_speaker=true → SHIFT/HIGH
   - 전부 완화형이고 hedge_speaker=false → REFLECT/MEDIUM + ALL_HEDGED
4. 유효한 새 방향이 전혀 없을 때만 LOW다.

사용자가 "중심이 바뀌었다"라는 문장을 직접 말해야만 새 방향인 것은 아니다.
현재 질문을 닫는 내용과 다른 기준을 여는 내용이 두 턴에 이어지면 같은 방향의 증거가 될 수 있다.

같은 방향은 같은 단어를 반복했다는 뜻이 아니다. 두 발화가 같은 새 질문에 답하는 기능을 하면
표면 표현이 달라도 묶는다. 단, 이 묶음을 근거로 사용자가 하지 않은 추상어를 clarification에 만들지 않는다.

- "예전 이유는 이제 아님" + "그만두면 지금 맡던 역할이 사라짐"
  → 그 활동을 지금 계속하는 이유라는 같은 방향 후보
- "현재 일이 오래 반복됨" + "앞으로 여기서 얻을 것이 없음"
  → 그 일을 계속할 미래 가치라는 같은 방향 후보
- "최근 행동이 나아짐·더 달라질 것" + "변화가 이어지는지 더 기다려보겠음"
  → 관계를 계속할지에서 변화 추이를 기다릴지로 옮겨가는 같은 방향 후보
- "현재 조건이 불만임" + "다른 선택지에서도 같은 문제가 반복될 수 있음"
  → 뒤 절은 실행 세부조건이 아니라, 선택을 바꿔도 문제가 풀리는지 묻는 상위 기준 후보

판정 예시는 다음처럼 구분한다.

  같은 금액·일정·스펙을 두 턴 반복                    → 세부조건이므로 LOW
  질문 범위 밖의 새 기준을 한 턴만 자발적으로 말함     → MEDIUM / SINGLE_SPONTANEOUS
  같은 새 기준을 서로 다른 두 턴에서 자발적으로 말함   → 단정형 조건까지 맞으면 HIGH
  AI가 제시한 방향에 동의만 함                         → LOW
  AI가 제시했지만 사용자가 자기 경험·사실을 새로 붙임  → MEDIUM / AI_LED_WITH_USER_MATERIAL

**MEDIUM일 때는 왜 MEDIUM인지를 medium_reason으로 함께 낸다.**

  SINGLE_SPONTANEOUS          자발 방향이 한 턴만 나왔다
  ALL_HEDGED                  같은 방향 자발 발화가 반복됐지만 전부 완화형이다
  AI_LED_WITH_USER_MATERIAL   AI가 먼저 연 방향에 사용자가 자기 재료를 덧붙였다

MEDIUM이 아니면 null이다.

**hedge_speaker가 true일 때 면제되는 것은 두 가지뿐이다.**

  - HIGH의 "1턴 이상 단정형" 요구
  - clarifications HIGH의 단정형 요구

말끝을 흐리는 것은 흔한 화법 습관이라, 그대로 두면 그 사용자의 세션은 통째로 빈다.
**단, 1-7의 한 턴 명시적 자기 선언에는 면제하지 않는다.**
한 턴짜리 근거에 완화형까지 허용하면 없는 이동을 만들 위험이 너무 커진다.

따라서 현재 창이 똑같아도, 같은 방향의 자발 발화 2턴이 모두 완화형인 경우
hedge_speaker=true면 SHIFT/HIGH, false면 REFLECT/MEDIUM이다.

### 1-5. 증거에서 제외하는 발화

다음은 사용자가 스스로 꺼내도, 두 턴 반복해도 증거로 세지 않는다.

- 자기 평가 — "내가 너무 예민한 건가", "내가 이상한가"
- 자기 진단 — "나 회피형인가", "완벽주의인가"
- 어릴 때 / 가족 / 결핍 / 성격

같은 발화 안의 **관찰된 사실**만 증거로 쓴다.

  U: 내가 말하면 반응이 없고 다른 사람이 같은 말 하면 그때 반응해.   ← 증거 O
     내가 너무 예민한 건가 싶기도 하고                              ← 증거 X

### 1-6. carryover

carryover는 창 밖으로 밀린 발화 중 이전에 MEDIUM으로 판정한 것이다.
각 항목에는 왜 MEDIUM이었는지가 medium_reason으로 붙어 있다.

  SINGLE_SPONTANEOUS / ALL_HEDGED
    → 창 안의 새 자발 발화와 같은 방향이면 합쳐서 2턴으로 센다

  AI_LED_WITH_USER_MATERIAL
    → 맥락 보존용이다. **자발 발화 횟수로 세지 않는다.**
      AI가 연 방향이므로, 사용자가 스스로 같은 방향을 다시 꺼내야 한다

**carryover가 있다는 것만으로 승격하지 않는다.** 창 안에 같은 방향의 자발적 발화가 반드시 있어야 한다.

창 안 발화의 자발성은 반드시 그 직전 AI 질문과 다시 비교한다. AI가 carryover의 방향을 직접 물었고
사용자가 그 질문에 답한 것은 같은 주제여도 새 자발 발화가 아니다. 앞서 말한 "AI가 따라 물어도
기존 발화의 자발성은 사라지지 않는다"는 규칙은 기존 발화를 보존할 뿐, AI가 물어서 나온 다음 답을
새 자발 증거로 바꾸지 않는다. 창 안에 별도의 범위 밖 절이 없다면 carryover와 합쳐 HIGH로 올리지 않는다.

### 1-7. 한 턴의 명시적 자기 선언

다음 네 가지를 **모두** 만족하면 1턴이어도 HIGH다.

1. 사용자가 AI가 제시하지 않은 대조·수정 구조를 스스로 꺼냈다 — "A가 아니라 B"
2. 단정적 자기 결론이다 — "~거네", "~거였어", "~구나"
3. B가 현재 중심 질문의 단순한 답이 아니라 Q1~Q3를 통과하는 새 방향이다
4. **직전 AI 질문이 A/B 대조나 B 후보를 먼저 제시하지 않았다**

  U: 사과를 받고 싶은 게 아니라 다음엔 같이 가자는 말을 듣고 싶은 거네

**AI가 먼저 "A예요, B예요?"처럼 대조를 던졌다면, 사용자가 B를 고른 한 턴은 최대 MEDIUM이다.**
4번이 없으면 이 예외가 Q4의 자발성 확인을 그냥 통과해버린다.
hedge_speaker도 이 예외를 완화하지 않는다.

**단, 그 구분이 이미 현재 중심 질문 안에 있으면 SHIFT가 아니라 CLOSE다.**

  현재 질문: 회사를 떠나고 싶은 걸까, 하는 일을 바꾸고 싶은 걸까?
  U: 회사가 싫은 게 아니라 이 반복되는 일이 싫은 거였어
  → 새 질문을 꺼낸 게 아니라 이미 있는 질문에 답한 것 → CLOSE

### 1-8. 판정 기준이 아닌 것

"회사 → 나", "상대 → 나"처럼 대상이 밖에서 안으로 옮겨가는 형태는
SHIFT에서 자주 관찰되지만 **판정 기준이 아니다.** 기준으로 쓰면 모든 고민을 내면 탐색으로 끌고 간다.

---

## 2. clarifications

같은 질문 안에서 실제로 선명해진 것만 넣는다. 사용자 표현의 범위를 좁혀 쓸 수 있지만
새 의미·원인·성향을 추가하면 안 된다.

AI가 먼저 제시한 해석어를 사용자가 "그런 것 같아", "그런 듯"처럼 되풀이만 했다면
사용자 표현으로 취급하지 않는다. 사용자가 별도의 관찰 사실을 덧붙이지 않은 한 그 단어를 저장하지 않는다.

  허용: "회사 자체가 싫은 것은 아님" / "가면 또 하긴 함"
  금지: "결국 성장 욕구가 충족되지 않는 것이 핵심"

**confidence는 원문의 확신 수준을 따른다.**

  U: 시간이 아까운 것 같아. 재미가 없는 건 아닌데
  → "재미가 없는 건 아님"  HIGH   (단정형)
  → "시간이 아까움"        MEDIUM (완화형)

hedge_speaker가 true이면 이 조건도 면제한다.

**중심 질문의 답을 좁히지 않는 사실은 clarification이 아니다.** 사용자가 답하긴 했지만
그 내용이 중심 질문을 한 칸도 움직이지 않으면 넣지 않는다. 특히 AI가 사실을 캐물어
받아낸 답이 그렇다. 화면의 "분명해진 것"은 고민이 좁혀진 자리를 보여 주는 칸이지
대화에서 오간 사실을 적는 칸이 아니다.

  중심 질문: 나 마음이 너무 힘들어
  AI: 크레가 죽은 줄 알았을 때, 바로 무엇을 확인했어요?
  U:  죽었는지 확인했지
  금지: "크레가 죽었는지 확인했다고 함"   ← 사실일 뿐 고민이 좁혀지지 않았다

clarification이 없는 턴은 정상이다. 빈 배열을 두려워해 사실 부스러기를 채우지 않는다.

Shift만 보수적으로 판정하고 clarification을 느슨하게 잡으면 일관성이 깨진다.

### invalidate_clarifications

사용자가 앞서 한 말을 번복하면 current_clarifications에 있는 해당 항목의 id를 넣는다.

  U2: 돈이 제일 크지                     → C1 저장됨
  U3: 근데 사실 돈보다 부모님이 더 어려워  → C1 무효화

무효화하지 않으면 틀린 정보가 화면에 계속 남는다.

---

## 3. branches

SHIFT가 아닌데 새 질문이 나왔고, **나중에 다시 생각해볼 가치가 있으면** 넣는다.

Branch 추출은 action confidence와 독립적이다. 현재 중심 질문의 답을 찾은 뒤 다시 원래 질문으로
돌아와야 하는 별도 결정이라면, action은 REFLECT/LOW인 채 branches에만 넣을 수 있다.
Branch가 있다는 이유로 그 방향을 MEDIUM Shift 후보로 올리지 않는다.

**Branch와 MEDIUM이 겹쳐 보이면 먼저 관계를 본다.**

- 현재 질문을 대체할 수 있는 새 기준·동기이며 아직 증거가 부족함 → MEDIUM, Branch에는 넣지 않음
- 해결한 뒤 현재 질문으로 돌아오는 별도의 "할까/어떻게 할까" 결정 → Branch, 그 자체의 action은 LOW

### 넣지 않는 것

- 정보 조회만으로 풀리는 질문
  "집주인은 월세를 얼마까지 올릴 수 있을까?" / "연구실 인건비는 얼마일까?"
  → 나중에 열었을 때 "정보를 찾아보세요"로 돌려보내게 된다

- 한 가지 사실 조건이 정해지면 자동으로 끝나는 조건부 행동
  "지원금이 나오면 알바를 그만두고, 안 나오면 계속한다"
  → 독립적으로 다시 생각할 질문이 아니라 원래 선택의 실행 조건이다

Branch는 한 사용자 턴만 떼어 보지 않고 창 전체로 판정한다. 처음에는 "그만둘까"처럼 보여도
다음 턴에 사용자가 한 가지 정보로 결정 규칙을 닫았다면 Branch를 만들지 않는다.

- 이번 턴에 MEDIUM으로 판정한 방향
  → 같은 것이 두 군데에 쌓인다

- 지금 풀 수 없고 다시 생각할 질문도 아닌 것
  "새 팀장은 어떤 사람일까?"

- 이미 입력의 pile에 있는 질문
  → pile을 확인하고 거른다

### promote_pile_item

pile에 있는 질문과 같은 방향의 자발적 발화가 HIGH 조건을 채우면 그 항목 id를 넣는다.

---

## 4. CLOSE

CLOSE는 해결 완료나 강제 종료가 아니라 사용자가 남길 만한 생각을 얻었다는 정리 제안이다.
최소 턴 수는 없다. 사용자가 현재 질문의 핵심 구분이나 달라진 생각을 자기 말로 정리했다면
새 정보가 나온 마지막 응답에서도 CLOSE할 수 있다. 중심 질문 이동은 먼저 SHIFT 조건을 따른다.

다음은 CLOSE 근거가 아니다: 단순 고민 요약, 짧은 답, 같은 말 반복, AI 해석에 대한 동의.
사용자가 이어서 생각하고 싶은 질문을 표현하면 그 질문을 이어가도록 REFLECT한다.
"방어적이다", "지쳤다"처럼 사용자 상태를 추정하지 않는다.

예: "연봉도 낮고 일이 재미없다는 거야"는 고민 요약이므로 REFLECT.
예: 현재 질문이 회사와 반복 업무의 구분이고 "회사가 아니라 반복 업무를 바꾸고 싶었던 거네"는 CLOSE.
예: "원하는 건 알겠는데 팀장에게 어떻게 말할지 더 생각하고 싶어"는 REFLECT.
AI가 먼저 만든 해석어에 "맞아"라고만 답한 것은 REFLECT/LOW이며 해석어를 저장하지 않는다.

### 정체

입력의 \`stalled\`는 코드가 센 값이다. 마지막 두 사용자 발화가 그 사람이 앞서 쓰던
길이에 견줘 둘 다 크게 짧아졌다는 뜻이고, 그것만을 뜻한다. 사용자가 지쳤는지 방어하는지는
이 값이 말해 주지 않고 너도 추정하지 않는다.

\`stalled: true\`이면 되묻기를 반복하지 말고 CLOSE를 제안할 수 있다. 위의 "짧은 답은
CLOSE 근거가 아니다"는 한 번의 짧은 답을 말한 것이고, 여기서는 코드가 연속을 확인했다.
같은 방향으로 되물어도 더 나오지 않는 상태이므로 사용자에게 멈출 자리를 준다.

\`stalled: false\`이면 이 조항은 없는 것으로 본다. 짧은 답 하나로 CLOSE하지 않는다.

### 질문이 닿지 않은 턴

입력의 \`confused\`는 사용자가 **직접** 방금의 질문을 못 알아듣겠다고 했거나 되묻기
자체를 그만하라고 썼다는 뜻이다. 코드가 사용자의 말에서 센 값이고, 그것만을 뜻한다.

\`confused: true\`인 턴은 **고민에 대한 답이 아니다.** 이 턴만으로 CLOSE하지 않고,
이 턴에서 clarification을 만들지 않으며, 중심 질문이 옮겨간 증거로도 쓰지 않는다.
대화가 멈춘 것이 아니라 질문이 빗나간 것이므로 REFLECT로 돌려보낸다.

\`confused: false\`이면 이 조항은 없는 것으로 본다.

입력에 이전 종료 제안을 거절한 시점의 발화가 있으면 사용자는 더 생각하기를 선택한 것이다.
그 발화와 의미가 같은 정리·바꿔 말하기로는 CLOSE하지 않는다. 그 이후 사용자 자신의
새로운 정리가 생긴 경우에만 다시 CLOSE한다. 계속 탐색 의사를 존중한다.
버튼은 "남기고 마치기" / "더 생각하기"이며 미해결 상태로도 남길 수 있다.

---

## 출력

JSON만 출력한다. 설명이나 코드펜스를 붙이지 않는다.

{
  "action": "SHIFT | REFLECT | CLOSE",
  "shift_confidence": "HIGH | MEDIUM | LOW",
  "medium_reason": "SINGLE_SPONTANEOUS | ALL_HEDGED | AI_LED_WITH_USER_MATERIAL | null",
  "evidence_turns": ["U2", "U3"],
  "clarifications": [
    { "text": "...", "confidence": "HIGH | MEDIUM", "evidence_turns": ["U3"] }
  ],
  "invalidate_clarifications": ["C1"],
  "branches": [
    { "text": "...", "evidence_turns": ["U3"] }
  ],
  "promote_pile_item": null
}

- clarifications는 한 응답에 최대 3개다. 핵심적인 새 사실을 우선하고 중복은 합친다. 생략한 사실을 보충하려고 새로운 의미를 만들지 않는다.
- branches는 최대 5개, 각 evidence_turns는 중복 없이 최대 10개다.
- action이 CLOSE면 shift_confidence는 생략한다.
- medium_reason은 REFLECT/MEDIUM일 때만 값을 넣고, 그 외에는 null이다.
- 없으면 빈 배열, promote_pile_item은 null.
- evidence_turns는 SHIFT·MEDIUM 판정의 근거가 된 사용자 발화다. LOW나 CLOSE면 비워도 된다.` +
  CLAIM_FIDELITY_RULE;

// ─────────────────────────────────────────────

export function buildJudgeUser(
  input: JudgeInput,
  hedgeSpeaker: boolean,
): string {
  const L: string[] = [];

  L.push(`현재 중심 질문: ${input.main_question}`);
  if (input.main_path.length > 1)
    L.push(`지나온 경로: ${input.main_path.join(" → ")}`);

  if (input.dismissed_closure)
    L.push(
      `이전 종료 제안을 거절한 시점의 사용자 발화: ${JSON.stringify(input.dismissed_closure)}`,
    );

  L.push(`hedge_speaker: ${hedgeSpeaker}`);
  if (hedgeSpeaker)
    L.push(
      `  (이 사용자는 대부분의 발화를 완화형으로 끝낸다. HIGH와 clarification의 단정형 조건을 면제한다)`,
    );

  L.push(`stalled: ${input.stalled === true}`);
  if (input.stalled)
    L.push(
      `  (마지막 두 발화가 이 사용자의 앞선 길이에 견줘 둘 다 크게 짧아졌다. 코드가 센 값이며 상태 추정이 아니다. CLOSE를 제안할 수 있다)`,
    );
  L.push(`confused: ${input.confused === true}`);
  if (input.confused)
    L.push(
      `  (직전 질문이 닿지 않았다고 사용자가 직접 썼다. 이 턴은 고민에 대한 답이 아니므로 CLOSE·clarification·이동 근거로 쓰지 않는다)`,
    );

  if (input.current_clarifications.length) {
    L.push(`\n지금까지 정리된 것`);
    for (const c of input.current_clarifications)
      L.push(`  ${c.id}  ${c.text}${c.confidence ? ` [${c.confidence}]` : ""}`);
  }

  if (input.pile.length) {
    L.push(
      `\n남겨둔 질문 (이미 보관됨 — 같은 질문을 branches에 다시 넣지 않는다)`,
    );
    for (const p of input.pile) L.push(`  ${p.id}  ${p.text}`);
  }

  if (input.carryover.length) {
    L.push(`\n창 밖으로 밀린 발화 (이전에 MEDIUM으로 판정)`);
    for (const c of input.carryover)
      L.push(`  ${c.turn}  [${c.medium_reason ?? "UNKNOWN"}]  ${c.text}`);
  }

  L.push(`\n최근 대화`);
  for (const t of input.turns)
    L.push(`  ${t.id}  ${t.role === "user" ? "사용자" : "AI"}: ${t.text}`);

  L.push(`\n판정하고 JSON 객체만 출력하라.`);
  return L.join("\n");
}
